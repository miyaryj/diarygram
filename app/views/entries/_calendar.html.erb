<%= month_calendar(table: {class: 'table'}) do |date| %>
  <%= date %><br/>
  <%= render(
    partial: 'entries/calendar_entries',
    locals: {date: date, entries: entries[date.to_s]}
  ) if entries.has_key?(date.to_s) %>
<% end %>
