<%= form_for(current_user.users_followed_users.find_by(followed_id: @user.id),
  html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Following", class: "btn btn-default pull-right" %>
<% end %>
